{
  "role_name": "role name",
  "app_key": "{{app_key}}",
  "app_install_parent_dir": "{{app_install_parent_dir}}",
  "app_version": "{{app_version}}",
  "app_sha256": "{{app_sha256}}",
  "app_file": "{{app_file}}",
  "app_group": "{{app_group}}",
  "app_user": "{{app_user}}"
}